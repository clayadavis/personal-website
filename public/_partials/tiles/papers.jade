ul
  - var papers = public._data.papers.papers
  each paper, idx in papers
    if idx == 0 || paper.year != papers[idx-1].year
      h3.li-sep
        = paper.year
    li
      != partial("../paper", {paper: paper})
